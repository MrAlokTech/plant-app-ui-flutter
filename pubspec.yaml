name: plant
description: A new Coding Challange from "HeyFlutter" by Alok Das .

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # ICONS
  cupertino_icons: ^1.0.2

  # MATERIAL ICONS
  material_symbols_icons: ^4.2671.0

  # INDICATOR
  smooth_page_indicator: ^1.1.0
  
  # SLIDER
  carousel_slider: ^4.2.1

  # STAGGERED LAYOUT
  flutter_staggered_grid_view: ^0.7.0

  # IMAGE
  cached_network_image: ^3.3.0

  # FONTS
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/
    - lib/assets/images/
